# 复制为同目录下的 `.env` 并填入真实值（`.env` 已被 git 忽略，勿提交仓库）。
#
# --- 阿里云百炼（DashScope）OpenAI 兼容模式（本项目顾问优先使用）---
# 官方说明：https://www.alibabacloud.com/help/zh/model-studio/qwen-api-via-openai-chat-completions
# 环境变量配置密钥：https://www.alibabacloud.com/help/zh/model-studio/configure-api-key-through-environment-variables
#
# 中国大陆（北京）地域示例；API Key 与地域需一致，勿混用新加坡 Key 调北京 URL。
DASHSCOPE_API_KEY=
# 可选：覆盖兼容模式 base（不含 /chat/completions 后缀）
# DASHSCOPE_COMPAT_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# 默认模型：qwen3.5-flash
# DASHSCOPE_MODEL=qwen3.5-flash
# 可选：按阶段覆盖模型（不填则回退到 DASHSCOPE_MODEL / OPENAI_MODEL）
# ADVISOR_INTENT_MODEL=qwen3.5-0.6b
# ADVISOR_PLANNER_MODEL=qwen3.5-flash
# ADVISOR_RESPONDER_MODEL=qwen3.5-flash
# ADVISOR_VERIFY_MODEL=qwen3.5-flash
# 可选：是否开启模型 thinking（默认 false，提升响应速度）
# ADVISOR_ENABLE_THINKING=false
# 可选：关闭 verify 阶段以降低时延（false=关闭，默认开启）
# ADVISOR_ENABLE_VERIFY=true
# 可选：单个搜索工具超时阈值（毫秒），超时会自动切换下一个搜索来源。
# ADVISOR_SEARCH_TOOL_TIMEOUT_MS=12000
# 可选：慢请求阈值（毫秒）；总耗时超过后打印慢请求诊断日志。<=0 表示关闭。
# ADVISOR_SLOW_REQUEST_THRESHOLD_MS=3000

# --- 可选：OpenAI 官方（仅当未配置 DASHSCOPE_API_KEY 时作为后备）---
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
#
# --- AgentLoop 工具：Tavily 搜索（可选，未配置时会跳过搜索步骤）---
# TAVILY_API_KEY=
#
# --- AgentLoop 工具：X API v2 最近搜索（可选，作为回退源）---
# 官方文档：https://docs.x.com/x-api/posts/search/quickstart/recent-search
# X_BEARER_TOKEN=
#
# --- AgentLoop 搜索回退顺序 ---
# 1) bailian-search (DASHSCOPE_API_KEY)
# 2) tavily-search (TAVILY_API_KEY)
# 3) x-search (X_BEARER_TOKEN)
#
# --- Flutter Web：页面在 http://localhost:<端口>，请求 API 为跨域；服务端已对 localhost / 127.0.0.1 放行。
# 运行 Web 时请带：--dart-define=API_BASE_URL=http://localhost:你的API端口（默认编译为 http://localhost:3000）
