import 'package:flutter/material.dart';

class PrivacyScopePage extends StatelessWidget {
  const PrivacyScopePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Privacy Scope'),
      ),
    );
  }
}
